---
title: "lush: why i built a wayland shell"
date: "2026-02-25"
description: ""
tags: ["wayland", "gtk", "lua"]
draft: false
---

# lush: why i built a wayland shell

If you have spent any time in the wayland ecosystem, you know the drill. You move away from X11, find a `compositor` which is similar to the `window manager` you used. Then you are faced with the "status bar problem". You want something that isnt just a static line of text but a dynamic, interactive and beautiful part of your desktop.

Ive been a long time user of **`AwesomeWM`** and **`Neovim`**. These tools share a core philosophy: **the tool should be a library, not just a binary**. You dont just configure them, you program them. When i moved to wayland, i missed that feeling of total control.

That's why i created **lush**

---

## what lush is

By "wayland shell" i mean a programmable GTK widget runtime for building bars, popups, notifications, desktop components, not a full a compositor/window manager.

lush is for people who liked the programmability of AwesomeWM, and wayland users who want to build their own desktop ui instead of stitching together fixed tools.

### tech

- batching engine

    lush's signal bus includes a native batching mechanism. This allows lush to update a lot of data in a single ui pass without a stutter.

- segmented markup
    
    Most status bars re-render the entire label when any part of it changes. lush's label can split a label into "segments" based on your `format` string. When for example your cpu usage changes, lush only updates the specific text fragment associated with that token.

- watchers

    lush's `process.watch` implements queue policies. Using it ensures that if you are running a heavy shell command, lush will never queue up multiple instances of that command.
    It protects your system from fork bombs if a script hangs. 


## alternatives

### AGS (Aylur's GTK Shell)

AGS is built on **GJS (Gnome Javascript)**. While it's very capable, it brings the entire `SpiderMonkey` engine to your status bar. It is powerful and mature but i wanted something with a smaller runtime model that feels closer to how i build desktop tooling.

### Eww (ElKowars wacky widgets)

Eww is a great project, but it relies on **Yuck**, a custom lisp like DSL. For my use case, i wanted a full language level extensibility instead of a widget dsl.

### Quickshell

kinda **Qt** :D


## the path forward

I just recently made the repo public because i feel like its getting to a state where other people could test it.

🌿 [lush](https://github.com/yrwq/lush)

```rust
while !light.exists() {
    bloom.anyway(); // :)
}
```

